name: build

on:
  push:
    branches: [master]
  pull_request:

jobs:
  build-and-test:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - name: Run unit tests
        run: ./tests/run.sh

      - name: Build app
        run: VERSION=ci ./build.sh
